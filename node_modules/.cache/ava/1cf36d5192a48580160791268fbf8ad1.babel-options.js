"use strict"

const process = require("process")

const defaultOptions = envName => {
  return {
    "babelrc": false,
    "plugins": [],
    "presets": [
      "/home/darth/Webs/balanced-brackets/node_modules/@ava/babel-preset-stage-4/index.js",
      [
        "/home/darth/Webs/balanced-brackets/node_modules/@ava/babel-preset-transform-test-files/index.js",
        {
          "powerAssert": false
        }
      ]
    ]
  }
}

const envOptions = Object.create(null)

exports.getOptions = () => {
  const envName = process.env.BABEL_ENV || process.env.NODE_ENV || "development"
  return envName in envOptions
    ? envOptions[envName]()
    : defaultOptions(envName)
}
